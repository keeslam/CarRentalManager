# Minimale nixpacks.toml - laat Nixpacks de defaults doen

[variables]
NIXPACKS_NODE_VERSION = "18"

# Override alleen de start command
[start]
cmd = "npm start"

# Optioneel: force production build
[phases.install]
cmds = ["npm ci --only=production"]

[phases.build]
cmds = ["npm run build"]
