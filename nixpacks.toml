# Coolify deployment configuration using Nixpacks
[variables]
NIXPACKS_NODE_VERSION = "20"

[phases.install]
cmds = ["npm ci"]
=======
[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm install"]


[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
